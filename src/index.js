import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Telling the src index to go to public folder index and run whatever is in the div element of id root
ReactDOM.render(<App />, document.getElementById('root'));

